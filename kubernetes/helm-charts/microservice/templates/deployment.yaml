apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "microservice.fullname" . }}
  labels: {{ include "microservice.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels: {{ include "microservice.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels: {{ include "microservice.labels" . | nindent 8 }}
    spec:
      serviceAccountName: {{ include "microservice.fullname" . }}
      securityContext: {{ toYaml .Values.securityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          # Container-level context. The pod-level securityContext above does
          # not prevent a container process gaining privileges through a setuid
          # binary; allowPrivilegeEscalation must be set on the container.
          securityContext: {{ toYaml .Values.containerSecurityContext | nindent 12 }}
          ports:
            - containerPort: {{ .Values.service.port }}
          livenessProbe: {{ toYaml .Values.livenessProbe | nindent 12 }}
          readinessProbe: {{ toYaml .Values.readinessProbe | nindent 12 }}
          resources: {{ toYaml .Values.resources | nindent 12 }}
